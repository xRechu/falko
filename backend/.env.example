# Medusa 2.0 – Backend (.env)
# Skopiuj do .env i uzupełnij wartości. W produkcji ustaw w Railway jako zmienne środowiskowe.

# Środowisko
NODE_ENV=development

# Publiczny URL backendu (np. https://twoj-projekt.up.railway.app)
# Jeśli Railway automatycznie dostarcza publiczny URL inną zmienną, ustaw BACKEND_PUBLIC_URL ręcznie.
BACKEND_PUBLIC_URL=http://localhost:9000
# (opcjonalnie, jeżeli Railway generuje publiczny URL przez inną zmienną)
# RAILWAY_PUBLIC_DOMAIN_VALUE=https://twoj-projekt.up.railway.app

# Baza danych (wymagane)
# Postgres: postgres://user:password@host:port/db
DATABASE_URL=postgres://postgres:postgres@localhost:5432/medusa

# Redis (opcjonalne – event bus / workflow)
# REDIS_URL=redis://default:password@localhost:6379

# CORS – dozwolone originy (frontend CloudPages, podgląd localhost itp.)
# Przykład dev: http://localhost:3000
ADMIN_CORS=http://localhost:3000
AUTH_CORS=http://localhost:3000
STORE_CORS=http://localhost:3000

# Sekrety (wymagane) – użyj długich losowych wartości w produkcji
JWT_SECRET=change-me-long-random
COOKIE_SECRET=change-me-long-random

# Tryb pracy (shared|worker|server)
MEDUSA_WORKER_MODE=shared
# Wyłączenie panelu admin (true/false)
MEDUSA_DISABLE_ADMIN=false

# Pliki – używamy lokalnego providera (static/). Brak zewnętrznego storage.

# Płatności – Stripe wyłączony (użyjemy Paynow plugin później)

# E-mail – korzystamy z Resend (wpisz swoje klucze)
RESEND_API_KEY=
RESEND_FROM_EMAIL=no-reply@yourdomain.com

# Wyszukiwanie – Meilisearch wyłączony
